%Simple MatLab graphics for book problems%Plots 5*(1 - exp(-t/10)) in steps of 0.2 for 0 < T < 50whitebg('w');T = 0:0.2:50;X = 5*(1-exp(-T/10)); %Type function to be plotted herefigure(1);F1 = plot(T, X, 'r-');xlabel('This is the label for the X axis');ylabel('This is the label for the Y axis');title('This is the title of the graph');set(F1, 'LineWidth', 2);  %This makes the plotting line two pixels wide